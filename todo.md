gemini provider model is not supported (choose the latest one[gemma 4])

@generate_keyword_map ->checkpoint logic is not really needed??
@check @generate_unit_embeddings.

llm gateway, (config abstraction + models.py).

